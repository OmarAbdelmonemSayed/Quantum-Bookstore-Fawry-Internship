package src.books;

public class DemoBook extends Book {
    public DemoBook() {}

    public DemoBook(String ISBN, String title, String author, int publicationYear, double price) {
        super(ISBN, title, author, publicationYear, price);
    }
}
